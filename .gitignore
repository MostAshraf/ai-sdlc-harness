.venv/
__pycache__/
*.pyc
.DS_Store
.claude/
# Local dev artifact for `qwen extensions link` — the published plugin is a
# Claude plugin (.claude-plugin/); qwen-extension.json is generated by the
# `install` converter and reproduced here only so `link` (which needs a native
# Qwen manifest) works on an uncommitted local branch.
qwen-extension.json
